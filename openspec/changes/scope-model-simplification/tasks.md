## 1. Baseline 與依賴順序前置

- [x] 1.1 建立健全度基線：跑 `npm run check` 與 `cargo test --manifest-path src-tauri/Cargo.toml --lib`，將 npm error 數（預期 0）與 cargo passing 數（預期 62）記入 `openspec/changes/scope-model-simplification/baseline.txt`。Verify: 該檔存在，記錄通過數與當前 warning 數作為下界。
- [x] 1.2 確認本機既有 `<project>/.felina/skills/*` 樣本：執行 `dir C:\MyProject\Pershing\felina\.felina\skills`（或對應指令）確認至少存在 `git` skill 子目錄，做為 migration 手動 smoke（task 6.x）的測試素材。Verify: 列出含 `SKILL.md` 的子目錄名稱清單記入 baseline.txt 附註。

## 2. 後端：移除 `SkillScope::Project` canonical 分支（對應 MODIFIED Requirement: Canonical Skill Storage、決策「取消 `SkillScope::Project` canonical（不保留死碼）」）

- [x] 2.1 （對應 MODIFIED Requirement: Canonical Skill Storage）在 `src-tauri/src/commands/canonical_skills.rs` 把 `canonical_skills_dir_for_scope(scope, project_path)` 收成 `canonical_skills_dir()`，回傳 `paths::felina_global_skills_dir()`，並更新內部 caller（`canonical_skills_list` / `canonical_skills_read` / `canonical_skills_write` / `canonical_skills_delete` 等）。所有需要 project_path 參數的 canonical CRUD 簽名一併簡化（不接受 scope 與 project_path）。Verify: cargo build exit 0；既有 cargo test 不退化；無對應 SkillScope::Project 的 canonical 分支殘留。
- [x] 2.2 [P] （對應 MODIFIED Requirement: Initial Skill Import）在 `src-tauri/src/commands/skill_import.rs` 移除 project-scope import 寫入路徑：`skill_import_apply` 與 `skill_import_scan` 與 `skill_import_scan_quick` 簽名拿掉 scope 參數（或內部固定 scope=global），寫入路徑固定 `~/.felina/skills/`。Verify: cargo build exit 0；既有 import wizard cargo test 不退化；scan 結果仍能列出非 global 來源 agent 目錄的 skill。
- [x] 2.3 [P] 在 `src-tauri/src/paths.rs` 移除 `felina_project_skills_dir`（除非 task 4.x migration 需要 read-only，否則完全刪除）；更新 `paths::tests::felina_project_skills_dir_under_project_root` test 為 migration 用的等價測試或刪除。Verify: cargo build exit 0；無 dead-code warning；test suite 不退化。
- [x] 2.4 在 `src-tauri/src/commands/fan_out/mod.rs` 確認 `resolve_pair` 路徑與 `SkillTarget.scope=project` 的 push 行為**完全沒動**，但在 doc comment 標註「`scope` 僅指 push 目的地、不再隱含主檔位置」。Verify: cargo build exit 0；fan_out 既有 cargo test 全部仍 pass；新增一個 doc test 或 unit test 確認 push global 主檔到 project agent dir 行為等價於 (b) 落地的測試。

## 3. 前端：取消 Skills 頁 Global/Project toggle（對應 MODIFIED Requirement: Registered Pages、決策「UI：取消 Global/Project toggle」）

- [x] 3.1 在 `src/lib/components/skills/SkillsPage.tsx` 移除 `ScopeToggle` 元件、移除 scope state；header actions 只留 List/Summary view-mode toggle、ProjectPicker（保留作 Skills 內無關 canonical scope 的選擇器或一併移除視 follow-up）、Reload、New skill。Verify: `npm run check` exit 0；Skills 頁 header 無 Global/Project toggle，且新增/編輯 skill 仍能完成。
- [x] 3.2 [P] 在 `src/lib/stores/skills-store.ts` 移除 `scope` state 與 `setScope` action（或重新定位為 view-mode state 配合 task 3.1）；caller 全部更新。Verify: `npm run check` exit 0；無 unused export。
- [x] 3.3 在 `src/lib/components/skills/SkillImportWizard.tsx` 與相關 store 移除 import target scope 選擇；wizard 一律 import 到 global、目標 target 由來源 agent dir 決定（沿用 `skill_import_apply` 邏輯）。Verify: `npm run check` exit 0；wizard UI 無 scope 選擇器；既有「multi-source defer」訊息仍可顯示（multi-source 解析屬 (c) 範疇）。
- [x] 3.4 [P] 在 `src/lib/components/skills/AddTargetDialog.tsx` 移除 canonical-scope 概念；該 dialog 只決定 target scope/project（一直如此，僅確認 prop 命名與 doc 不帶 canonical scope 字樣）。Verify: `npm run check` exit 0；dialog 行為與 (b) 落地版本一致。

## 4. 一次性 migration command（對應 ADDED Requirement: Project Canonical Migration、決策「一次性 migration CLI」）

- [x] 4.1 （對應 ADDED Requirement: Project Canonical Migration）在 `src-tauri/src/commands/` 新建 `migration.rs`（或重用既有合適模組）並定義 `MigrationCandidate { project_path: String, skill_name: String, conflict: bool }`、`MigrationAction { project_path: String, name: String, action: "keep"|"overwrite"|"skip" }`、`MigrationResult { project_path: String, name: String, status: "applied"|"skipped"|"conflict-resolved", target_path: String }`。（實作差異：`MigrationAction`/`MigrationResult` 比原規劃多帶 `project_path`，因為同一 skill name 可能跨多個 project，僅靠 name 無法 unambiguously 指認 candidate。）Verify: cargo build exit 0。
- [x] 4.2 [P] 實作 `migrate_project_canonicals_scan() -> Vec<MigrationCandidate>`：呼叫 `known_projects_list` 列舉所有 project，對每個 project 掃 `<project>/.felina/skills/<name>/SKILL.md`（即 task 2.3 保留的 read-only path helper），對每個 candidate 檢查 `~/.felina/skills/<name>` 是否存在（conflict 旗標）。在 `mod.rs` / `lib.rs` 註冊 command。Verify: cargo build exit 0；cargo test — 預先建一個假 project + 假 global skill，scan 後 conflict 旗標正確。
- [x] 4.3 [P] 實作 `migrate_project_canonicals_apply(items: Vec<MigrationAction>) -> Vec<MigrationResult>`：對每個 item：(a) action=skip → 跳過；(b) action=keep 且 conflict → 跳過、回 status="conflict-resolved"；(c) action=keep 且無 conflict → 複製 SKILL.md 與 bundled siblings 到 `~/.felina/skills/<name>/`，加 SkillTarget `{scope:project, project:<原 project path>, agent: <繼承自原 sync-meta 或 anthropic 預設>, enabled: true, mode: tracked}` 到新主檔的 sync-meta；(d) action=overwrite → 強制覆寫 global 主檔，target 同 (c)；任何情況下都不刪除原 `<project>/.felina/skills/<name>/` 目錄。Verify: cargo test — 兩個 candidate（一個 conflict、一個非 conflict）apply 後：global 出現主檔、target 含 scope=project 指向原 project；原目錄仍存在。
- [x] 4.4 在 `src/lib/tauri/commands.ts` 加 `api.migration.{scan,apply}` wrapper，並在 `src/lib/types/skills.ts` 加對應 TS type。Verify: `npm run check` exit 0；wrapper 簽名與後端對齊。
- [x] 4.5 在 Projects view（task 5.x）偵測選中 project 含 legacy `<project>/.felina/skills/*` 時，顯示 onboarding 提示「此 project 有舊版主檔，可一鍵 migrate 到 global」+ 按鈕觸發 `migration.scan`+`migration.apply` 流程，顯示 conflict 處理選單（keep / skip / overwrite）。Verify: `npm run check` exit 0；smoke 確認本機 felina project 的 git skill migrate 後出現在 `~/.felina/skills/git/` 且 target 含對應 project。

## 5. 前端：Projects top-level view（對應 ADDED Requirement: Projects Top-Level View + Managed Inventory View、決策「Projects view 兩欄佈局」+「納管清單行 = union」+「動作集 D2」）

- [x] 5.1 在 `src/lib/router.tsx` 新增 `/projects` route，lazy-load `ProjectsPage`；更新 `NAV_ITEMS`、`Page` type、`PAGE_TITLES`、`PAGE_DESCRIPTIONS` 五處保持一致（對應 MODIFIED Registered Pages 的 5 entries 檢查）。Verify: `npm run check` exit 0；sidebar 看到 Skills / Projects / Settings / Templates / Memory 五個 entries；Command Palette 同步列出。
- [x] 5.2 [P] （對應 ADDED Requirement: Projects Top-Level View）在 `src/lib/components/projects/ProjectsPage.tsx` 實作頁面 shell（含 PageHeader「Projects」與兩欄 grid layout `grid-cols-[280px_minmax(0,1fr)]`）；管理 `selectedProjectPath` state（預設 L1 cwd）、把選中值傳給左右欄。Verify: `npm run check` exit 0；route 進得去、看到兩欄佈局。
- [x] 5.3 [P] （對應 ADDED Requirement: Projects Top-Level View + MODIFIED Requirement: Known Projects Model）在 `src/lib/components/projects/ProjectsList.tsx` 實作左欄：呼叫 `known_projects_list` 取 KnownProject[]，按 alphabetical normalized path 排序渲染；每個 entry 顯示 path + 來源 chip（cwd/detected/saved）；`exists=false` 顯示「⚠ project not found」灰字（重用 (b) 的 `AlertTriangle`）；選中 highlight；空清單顯示 empty state 並提示「Browse 加入 / 去 Skills 管 global」。Verify: `npm run check` exit 0；smoke 看到本機 felina cwd 預設選中、若刪除某 saved entry 對應資料夾後重整顯示 not-found。
- [x] 5.4 （對應 ADDED Requirement: Managed Inventory View）在 `src/lib/components/projects/ManagedInventory.tsx` 實作右欄 union 邏輯：選中 project 變動時 invoke 三條 API（`skill_import_scan` for 選中 project、`canonical_skills_list` 取 global 主檔、`known_projects_list` 取 KnownProject for `exists` flag）；前端組 union by skill name；對每行算「managed 標籤」（global 中有對應 skill 且其 target 含 `{scope:project, project:<selected>}`）+ per-agent chip × 3（present iff scan 結果中該 agent 含此 skill name）。Verify: `npm run check` exit 0；smoke 設計：在 projectA 的 .claude 放一個 skill、global 不放 → 該行 Unmanaged + claude chip present、其它 chip absent。
- [x] 5.5 [P] 在 ManagedInventory 渲染每行 + D2 動作：Unmanaged 行右側 `[Import to global]` 按鈕 → 呼叫 `skill_import_apply`（複用既有 ImportCandidate shape）；Managed 行整列可點 → navigate 到 `/skills?select=<skill-name>`（或對等 state mechanism）並選中該 skill。Verify: `npm run check` exit 0；smoke：Unmanaged 行 Import 成功後該行變 Managed（onSuccess 重新 fetch 三條 API）；Managed 行點擊跳 Skills 並選中。
- [x] 5.6 [P] 在 Skills 頁實作「URL 帶 skill name 自動選中」入口以支援 task 5.5 的跳轉（讀 query param 或 location state，選中對應 skill）。Verify: `npm run check` exit 0；smoke：手動造 `/skills?select=git` URL 進入時 git skill 被自動選中於 SkillList。

## 6. 整合與最終驗證

- [x] 6.1 跑 `npm run check`（0 error）、`cargo build --manifest-path src-tauri/Cargo.toml`（exit 0、無新 warning vs baseline）、`cargo test --manifest-path src-tauri/Cargo.toml --lib`（不退化 + 新增 migration tests 全 pass）。Verify: 三項通過、cargo test 數量 ≥ 66 (baseline 62 + ≥4 個 migration tests)。
- [x] 6.2 [P] 跑 `spectra validate scope-model-simplification` 與 `spectra analyze scope-model-simplification`：Critical = 0、Warning = 0。Verify: validate ✓、analyze Clean。
- [ ] 6.3 手動 smoke（使用者書面回報）：(a) sidebar 看到 Skills / Projects / Settings / Templates / Memory 五個 entries，Skills 頁無 Global/Project toggle；(b) Skills 頁新建 skill 出現在 `~/.felina/skills/<name>`；(c) Projects 頁左欄列出 Known Projects、L1 預設選中、exists=false 條目顯示警示；(d) 選中含 legacy `.felina/skills/git/` 的 felina project → 右欄列出 git 行 + Unmanaged 標籤 + claude chip present；(e) 觸發 migration 流程 → git skill 出現在 `~/.felina/skills/git/`、target 含 `{scope:project, project:<felina path>}`、git 行變 Managed；(f) 點 Managed 行跳到 Skills 並選中 git；(g) Projects 頁針對另一個未管理 skill 直接 Import to global → 同流程觸發。Verify: 使用者書面回報 (a)–(g) 行為符合預期。
