## 1. Baseline 與型別骨架

- [x] 1.1 建立健全度基線:跑 `npm run check` 與 `cargo test --manifest-path src-tauri/Cargo.toml --lib`,將 stdout / stderr 與 exit code 寫入 `openspec/changes/path-bug-and-target-model/baseline.txt`。Verify: 該檔存在;npm 段記錄當前 error 數作為 diff 基準、cargo 段記錄當前 passing test 數作為下界基準。
- [x] 1.2 [P] 在 `src/lib/types/skills.ts` 新增 per-skill target TS 型別(對應 ADDED Requirement: Per-Skill Target Model):`SkillTarget`(agent / scope / project? / enabled / mode)、`TargetMode`(`tracked` | `detached` | `forked`)、`SyncMetaV2`(version / targets / last_sync / dirty)、`LastSyncEntry`(pushed_hash / base_snapshot? / at)。後端 wire 形狀對齊以 camelCase 暴露。Verify: `npm run check` 對該檔 0 error。

## 2. Path 反解修復（Requirement: Project Path Resolution）

- [x] 2.1 在 `src-tauri/src/paths.rs::project_hash_to_path` 補 Windows drive-letter 解碼路徑(對應 Requirement: Project Path Resolution + 決策 Path 反解修復（Windows drive-letter + unresolved fallback）):當 hash 開頭為「單字母 ASCII + 雙減號」(例如 `C--`)時,還原為該 drive-letter 起頭的路徑(`C:/...`),其餘段以 `/` 連接。`cwd_from_session_file` 仍為優先來源;drive-letter 解碼為其後備。Verify: 餵 `C--MyProject-Pershing-felina` 應回 `C:` 起頭路徑而非 `C//...`;餵不含 drive-letter 的 hash 行為不變。
- [x] 2.2 在 `src-tauri/src/paths.rs::resolve_segments` 支援 drive-letter 起頭(對應 Scenario: Windows drive-letter hash is decoded):smart segment resolve 不再寫死 POSIX `/` 為起點,當 hash 偵測到 drive-letter 時以該 drive root(`C:/`)為起點遞迴匹配真實目錄;無 drive-letter 時行為不變。Verify: 對含 `-` 的真實 Windows 子目錄名(例:`my-dir`)能正確 group segments;對 POSIX 路徑回歸測試不退化。
- [x] 2.3 新增 unresolved 表示與防呆(對應 Scenario: Unresolvable hash is reported, not guessed):`project_hash_to_path` 在三條策略皆無法對應到實際存在目錄時,回傳明確的 unresolved(Rust 改回 `Option<String>` 或新增 enum `ResolvedProject::{Resolved(String), Unresolved(hash)}`);所有呼叫端(目前為 `memory_dir` 等)在收到 unresolved 時 SHALL 不當作合法路徑使用。Verify: cargo test 餵不可解 hash 取得 unresolved;grep 確認沒有呼叫端把 unresolved 字串拼進 fs::write/read_dir 路徑。
- [x] 2.4 [P] 撰寫 cargo test 於 `src-tauri/src/paths.rs::tests`(對應上述三個 Scenario):(a) `decodes_windows_drive_letter_hash`(餵 `C--<existing-dir-name>` 在臨時 tempdir 鏡像下回 `C:` 起頭);(b) `prefers_jsonl_cwd_over_decoding`(.jsonl 有效 cwd 優先,不走 decoding);(c) `reports_unresolved_for_bogus_hash`(全失敗回 unresolved 變體)。Verify: 三個 test 通過、baseline cargo test 數至少 +3。

## 3. sync-meta schema v2 + backfill（Requirement: Per-Skill Target Model）

- [x] 3.1 在 `src-tauri/src/commands/canonical_skills.rs` 新增 v2 結構與序列化(對應決策 sync-meta schema v2 與 target 結構 + ADDED Requirement: Per-Skill Target Model):`SyncMetaV2 { version: u32, targets: Vec<SkillTarget>, last_sync: BTreeMap<String, LastSyncEntry>, dirty: bool }`;`SkillTarget { agent: AgentId, scope: SkillScope, project: Option<String>, enabled: bool, mode: TargetMode }`;`TargetMode` enum tracked/detached/forked(`forked` 標 `#[serde(rename = "forked")]` 但本 change 不在 fan-out 使用);`LastSyncEntry { pushed_hash: String, base_snapshot: Option<String>, at: String }`。`version` 欄位以 u32 序列化為 `2`。Verify: cargo build exit 0;serde round-trip 一筆 v2 sample,結構與欄位名(camelCase wire / snake_case Rust)正確。
- [x] 3.2 實作 v1 → v2 backfill 讀取(對應決策 既有 skill 的 v1 → v2 backfill + Scenario: Legacy v1 sidecar is backfilled at read time):新增 `read_sync_meta_v2(skill_dir, skill: &CanonicalSkill, scope, project_path)`,當磁碟上 sidecar 缺 `version` 欄位或 `targets` 欄位視為 v1,按 skill `agents` 欄位 × 該 skill 所屬 scope/project 衍生 tracked 啟用 target,並保留 v1 的 `dirty` / `last_synced`(若有,放進 backfilled 第一個 target 的 `last_sync` 條目作為粗略對應,或丟棄 — 由實作決定但 cargo test 鎖定可觀察行為)。原 `read_sync_meta` 保留為內部 helper 供 backfill 取得 v1 欄位。Verify: cargo test 覆蓋 Scenario「Legacy v1 sidecar is backfilled at read time」的兩個 backfilled target 與保留 dirty/last_synced 的條件。
- [x] 3.3 [P] 實作 v2 sidecar 寫入(對應決策 sync-meta schema v2 + Scenario: New v2 sidecar round-trips):新增 `write_sync_meta_v2(skill_dir, &SyncMetaV2)`,序列化為 JSON 寫入 `.felina-sync-meta.json`,覆蓋現有檔(整檔覆寫,不做欄位 merge)。維持既有 `mark_sync_meta_dirty(skill_dir)` 行為:在 v2 sidecar 下將 `dirty` 翻為 true,保留 `targets` 與 `last_sync`。Verify: cargo test 寫一筆 v2 sidecar 後磁碟 JSON 含 `"version": 2` 與 targets 陣列;`mark_sync_meta_dirty` 不弄丟 targets。
- [x] 3.4 [P] 在 `src/lib/types/skills.ts` 對齊 v2 wire 型別與既有 `CanonicalSkill`(對應 1.2 的型別 + Per-Skill Target Model):`SyncMetaV2` 對應 Rust 結構;`CanonicalSkill` 增加 `targets: SkillTarget[]` 與 `lastSync: Record<string, LastSyncEntry>` 欄位(由後端 list/read 帶出),`dirty` 與 `lastSynced`(粗略,沿用既有 UI 顯示)維持。Verify: `npm run check` 0 error;前端既有 `skills-store.ts` / `SkillList.tsx` 對 dirty/lastSynced 的讀取仍可運作。

## 4. Fan-out 切換為 targets 驅動（Requirement: Fan-Out to Agent Targets - MODIFIED）

- [x] 4.1 修改 `src-tauri/src/commands/fan_out/mod.rs::skill_sync_one` 改由 targets 驅動(對應 MODIFIED Requirement: Fan-Out to Agent Targets + 決策 fan-out 由 target 清單驅動 + 決策 本 change target 來源等同 agents 欄位（不引入空預設）):讀取 sidecar v2(或 backfill v1),逐 target 處理;跳過 `enabled: false` 與 `mode: detached` 的 target;以 target 的 scope/project 與 `agent_paths_get()` 解析 target 目錄;呼叫對應 renderer + 既有的 `copy_bundled_siblings`。本 change 中 `forked` mode 視同 `detached`(不寫入,留 placeholder)。Verify: cargo test 餵一個 backfilled v1 skill,push 結果 SyncResult 數量、agent、scope 與既有「按 agents 欄位 push」一致(行為等價)。
- [x] 4.2 修改 `skill_sync_one` 在 per-target push 成功後更新 `last_sync[targetKey]`(對應 MODIFIED Fan-Out 末段「After a successful per-target write...」):計算 rendered 內容 hash(SHA-256 hex of 寫入後 SKILL.md 內容)、寫入時間戳(沿用 `current_iso8601`)、更新 `pushed_hash` 與 `at`;單一 target 失敗不更新該 target 的 `last_sync` 但其他 target 照常更新。Verify: cargo test 對含三 target 的 skill push,sidecar 的 `last_sync` 有三筆 entry 含對應 `pushed_hash`/`at`;模擬一個 target 寫入失敗,該 target 無 entry、其他兩個有。
- [x] 4.3 [P] 修改 `skill_sync_all` 配合 v2:`skill_sync_all` 仍走 `canonical_skills_list` + 逐 skill `skill_sync_one`,沿用既有 per-target 錯誤隔離。Verify: cargo test 同時對兩個 skill 跑 sync_all,各 skill sidecar 都更新到 v2 last_sync;不再有任何呼叫從 SKILL.md frontmatter 的 `agents` 欄位直接判斷 fan-out target。
- [x] 4.4 [P] 驗證 `enabled: false` / `mode: detached` 跳過(對應 Scenario: Disabled or detached targets are skipped):cargo test 構造一個包含 enabled false 與 detached target 的 v2 sidecar,sync_one 後該兩 target 不寫入 agent 目錄,其餘 enabled tracked target 正常寫入並更新 `last_sync`。Verify: 新 test 通過、加入 cargo test pass 數。

## 5. 整合與最終驗證

- [x] 5.1 對照 `baseline.txt` 跑 `npm run check`(不得新增 baseline 外的 error)與 `cargo build --manifest-path src-tauri/Cargo.toml`(exit 0、無新 warning;tokens/ 子系統 pre-existing warning 不算)。Verify: 兩項自動檢查通過、warning 數不超過 baseline。
- [x] 5.2 跑 `cargo test --manifest-path src-tauri/Cargo.toml --lib`(對應 Acceptance criteria 全部 cargo test):確認本 change 新增的 paths + sync-meta + fan-out tests 全綠且既有 multi-agent-skills-foundation 35 test 不退化(下界 35 + 本 change 新增數)。Verify: 全部 passing、無 failed/ignored。
- [x] 5.3 跑 `spectra validate path-bug-and-target-model` 與 `spectra analyze path-bug-and-target-model`(對應 design 的「無 Critical / Warning」收尾):確認 Critical = 0、Warning = 0;Suggestion 可保留(不阻擋)。Verify: validate 回 valid;analyze JSON 的 Coverage / Consistency / Gaps 為 Clean、Ambiguity 不引入新 Critical/Warning。
- [x] 5.4 [P] 手動 smoke(對應 Behavior「升級後既有 skill 的 push 目標與現況相同」):`npm run tauri dev` 啟動,選一個既有(v1 sidecar)skill 按 Push,觀察 push 結果三家 target(若 agents=三家)的 path 與 push 成功訊息;確認 `~/.felina/skills/<name>/.felina-sync-meta.json` 升級成 v2(有 `version: 2` + `targets` + `last_sync`)並保留 dirty/last_synced 含意。Verify: 使用者書面回報 push 行為與升級前一致、sidecar 升級為 v2 結構。
