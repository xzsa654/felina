## 1. Baseline 與型別骨架

- [x] 1.1 建立健全度基線:跑 `npm run check` 與 `cargo test --manifest-path src-tauri/Cargo.toml --lib`,將 stdout / stderr 與 exit code 寫入 `openspec/changes/known-projects-and-multi-target/baseline.txt`。Verify: 該檔存在;npm 段記錄當前 error 數(預期 0),cargo 段記錄當前 passing 數作為下界(預期 44)。
- [x] 1.2 [P] 在 `src/lib/types/skills.ts` 新增 known-projects 與 target editor 相關 TS 型別(對應 ADDED Requirement: Known Projects Model + Per-Skill Target Editor):`ProjectSource`(`"cwd" | "detected" | "saved"`)、`KnownProject`(`path: string; sources: ProjectSource[]`)、`OrphanFile`(`path: string` + agent/scope 標示),camelCase wire 對齊。Verify: `npm run check` 對該檔 0 error。

## 2. Known Projects 後端（Requirement: Known Projects Model + Known Projects Mutation；對應決策「Known Projects 三來源 + minimal storage」）

- [x] 2.1 在 `src-tauri/src/commands/known_projects.rs` 新建 capability 後端骨架(對應 ADDED Requirement: Known Projects Model + 決策「Known Projects 三來源 + minimal storage」):定義 Rust `KnownProject { path: String, sources: Vec<ProjectSource> }` 與 `ProjectSource` enum(`Cwd` / `Detected` / `Saved`,camelCase wire);新增 normalized-path helper(絕對路徑、`/` 分隔、去尾斜線、Windows casefold)供 dedupe 用。Verify: cargo build exit 0;normalized-path helper 的 unit test 對 `C:/proj/foo/`、`c:/proj/foo`、`C:/proj/foo` 三者 normalize 後相等。
- [x] 2.2 實作 `known_projects_list(current_project: Option<String>)` 三來源合併(對應 Scenario: Three sources merge and deduplicate + Scenario: Unresolved hash is excluded + Scenario: Missing or malformed store yields cwd and detected only):L1 = current_project(source Cwd);L2 = 列舉 `~/.claude/projects/<hash>` 經 `paths::project_hash_to_path` 反解,unresolved 略過(source Detected);L3 = 讀 `~/.felina/known-projects.json` 的 `projects` 陣列(source Saved);union 後 by normalized path dedupe,合併 sources。檔缺失 / 非 JSON / 缺 `projects` 鍵 → L3 視為空、不報錯。Verify: cargo test 覆蓋三 Scenario — 合併去重 + source 標記正確、unresolved hash 不入清單、malformed store 只回 cwd+detected。
- [x] 2.3 [P] 實作 `known_projects_add(path)` 與 `known_projects_remove(path)`(對應 ADDED Requirement: Known Projects Mutation + Scenario: Adding an existing project is idempotent + Scenario: Removing a saved project deletes only that entry):只讀寫 `~/.felina/known-projects.json` 的 `projects` 陣列;add 時若已存在同 normalized path 則 no-op;remove 刪 normalized 相符者、其餘不動。Verify: cargo test — add 重複(含尾斜線 / 大小寫變體)後陣列不變、remove 後只剩其他 entry。
- [x] 2.4 在 `src-tauri/src/commands/mod.rs` 宣告 `known_projects` module、在 `src-tauri/src/lib.rs` 的 `invoke_handler!` 註冊 `known_projects_list` / `known_projects_add` / `known_projects_remove` 三條 command。Verify: cargo build exit 0;grep 確認三條 command 出現在 invoke_handler。

## 3. Target 來源切換：empty default + agents-derived 退役（Requirement: Per-Skill Target Model - MODIFIED；對應決策「Target 來源:empty default + 顯式編輯(agents-derived 退役)」）

- [x] 3.1 在 `src-tauri/src/commands/canonical_skills.rs` 移除 agents-derived 對齊(對應 MODIFIED Per-Skill Target Model + 決策「Target 來源:empty default + 顯式編輯(agents-derived 退役)」+ Scenario: New skill is created with empty targets):刪除 `align_v2_targets_to_agents` 函式與 `canonical_skills_write` 對它的呼叫;新建 skill 寫入的 sync-meta sidecar `targets` 為空陣列(`mark_sync_meta_dirty` 行為維持)。Verify: cargo test — `canonical_skills_write` 新建 skill 後讀 sidecar,`targets` 為空、`version: 2`;既有 `write_marks_canonical_dirty_in_sync_meta` 不退化。
- [x] 3.2 在 `read_sync_meta_v2` 移除「v2 + 空 targets → 從 agents backfill」分支(對應 Scenario: Empty v2 targets are not backfilled from agents):v2 + 空 targets 直接回空清單(視為「使用者尚未加 target」);v1 sidecar(無 version / 無 targets)仍走一次性 agents backfill(對應 Scenario: Legacy v1 sidecar is backfilled once at read time)。Verify: cargo test — v2+空 targets 讀回為空且不含任何 agents-derived target;v1 sidecar 讀回仍 backfill 出與 agents 一致的 tracked targets 並保留 dirty/last_synced。
- [x] 3.3 [P] 在 `src/lib/components/skills/SkillEditor.tsx` 移除 agents checkbox 區塊(對應 MODIFIED Visual Frontmatter Editor + Scenario: Agents field is not an editable control):表單不再渲染 agents 控制項;save 時保留既有 `agents` 值原樣寫回 frontmatter(不清空、不變動)。Verify: `npm run check` 0 error;手動確認 editor 無 agents 控制項、save 後 SKILL.md 的 agents 欄位不變(此項記入 task 7 smoke)。

## 4. Per-skill target editor（Requirement: Per-Skill Target Editor；對應決策「Target editor UI:list + add-target dialog」）

- [x] 4.1 在 `src-tauri/src/commands/canonical_skills.rs`(或 fan_out 模組,擇 targets 讀寫所在處)新增 `skill_targets_set(scope, project_path, skill_name, targets: Vec<SkillTarget>)`(對應 ADDED Per-Skill Target Editor + 決策「Target editor UI:list + add-target dialog」+ Scenario: Add a target to an empty skill + Scenario: Removing a target prunes its last_sync entry):整批覆寫 sidecar `targets`;保留 `last_sync` 中 key 仍對應到新清單 target 的 entry、prune 掉孤兒 key;翻 dirty=true。在 `mod.rs` / `lib.rs` 註冊 command。Verify: cargo test — set 後 targets 等於傳入值;移除一個 target 後其 last_sync key 不存在、保留 target 的 entry 仍在。
- [x] 4.2 [P] 在 `src/lib/components/skills/AddTargetDialog.tsx` 新建 add-target dialog(對應 Per-Skill Target Editor + design「list + add-target dialog」):選 agent(三家)+ scope(global / project);scope=project 時 project 下拉用 `api.knownProjects.list` 結果,但只有「當前 project」可選,其餘 entry disabled 並附「cross-project: Phase 1.5 (b)」提示。確認後回傳一筆預設 Tracked 的 `SkillTarget`。Verify: `npm run check` 0 error;dialog 對非當前 project 的 entry 呈現 disabled。
- [x] 4.3 在 `src/lib/components/skills/TargetEditor.tsx` 新建 target list 元件(對應 Per-Skill Target Editor + Scenario: Setting a target to Disabled excludes it from push):逐 target 一行顯示 agent / scope / project + segmented control(Tracked / Detached / Disabled,內部 flatten 到 enabled+mode;第四格 Forked disabled + tooltip「Phase 2」);空清單顯示 empty state;每行 trash icon 刪除;`[+ Add target]` 開 AddTargetDialog;變更後呼叫 `api.skillTargets.set`。Verify: `npm run check` 0 error;segmented control 三態與 enabled/mode 對應正確(Tracked=enabled+tracked、Detached=enabled+detached、Disabled=!enabled)。
- [x] 4.4 [P] 在 `src/lib/tauri/commands.ts` + `src/lib/stores/skills-store.ts` 接線(對應 Per-Skill Target Editor):`commands.ts` 加 `api.knownProjects.{list,add,remove}` 與 `api.skillTargets.set`;store 加 set 後 `loadEntries()` 刷新。`SkillsPage.tsx` 在 skill 詳情區掛入 `TargetEditor`。Verify: `npm run check` 0 error;前端既有 dirty / Sync info bar 對空 targets skill 正確隱藏(沿用既有邏輯)。

## 5. 顯式 orphan prune（Requirement: Explicit Orphan Prune）

- [x] 5.1 新增 `skill_prune_orphans_scan(scope, project_path, skill_name) -> Vec<OrphanFile>`(對應 ADDED Explicit Orphan Prune + Scenario: Scan identifies orphaned agent files):列舉該 skill 在 `agent_paths_get` 解析的 global + 當前 project agent 目錄,找出存在 `<agentdir>/<skill>/SKILL.md` 但對應 target 不在清單(或 mode 為 detached / disabled)的殘留檔,回傳路徑;不刪除。在 `mod.rs` / `lib.rs` 註冊。Verify: cargo test — 移除 gemini target 後 scan 含該 gemini SKILL.md 路徑;仍 tracked 的 target 對應檔不入結果。
- [x] 5.2 新增 `skill_prune_orphans_apply(scope, project_path, skill_name, paths: Vec<String>)`(對應 Scenario: Prune deletes only confirmed orphans + Scenario: Toggling Detached does not auto-delete):刪除傳入確認的孤兒檔連同其 skill 子目錄,逐檔錯誤隔離;不在 target toggle Detached 時自動觸發。Verify: cargo test — apply 兩個確認路徑後該兩檔與子目錄被刪、仍在清單的 target 對應檔保留;另一 test 確認 `skill_targets_set` 切 detached 後對應 agent 檔仍在磁碟(需 apply 才刪)。
- [x] 5.3 [P] 在 `src/lib/components/skills/SkillsPage.tsx`(或 TargetEditor)加 `[Prune orphans]` 按鈕 + 接線(對應 Explicit Orphan Prune):點擊先呼叫 `api.skillPrune.scan`,以共用 `shared/ConfirmDialog` 列出待刪完整路徑,確認後呼叫 `api.skillPrune.apply` 並 `loadEntries()` 刷新;`commands.ts` 加 `api.skillPrune.{scan,apply}`。Verify: `npm run check` 0 error;無孤兒時 scan 回空、按鈕給「No orphans found」回饋(不開空 dialog)。

## 6. 整合與最終驗證（自動 gate）

- [x] 6.1 對照 `baseline.txt` 跑 `npm run check`(不得新增 baseline 外 error)與 `cargo build --manifest-path src-tauri/Cargo.toml`(exit 0、無新 warning;tokens/ 子系統 pre-existing warning 不算)。Verify: 兩項通過、warning 數不超過 baseline。
- [x] 6.2 跑 `cargo test --manifest-path src-tauri/Cargo.toml --lib`(對應 Acceptance criteria 全部 cargo test):確認本 change 新增的 known_projects + target editor + prune tests 全綠,且既有 path-bug-and-target-model + multi-agent-skills-foundation 44 test 不退化。Verify: 全部 passing、無 failed/ignored。
- [x] 6.3 跑 `spectra validate known-projects-and-multi-target` 與 `spectra analyze known-projects-and-multi-target`:確認 Critical = 0、Warning = 0;Suggestion 可保留。Verify: validate 回 valid;analyze Coverage / Consistency / Gaps 為 Clean。

## 7. Dirty 語意統一 + UI 流程修正（Session 2 smoke 回饋）

- [x] 7.1 修正 `SkillEditor` 新 skill 預設 `agents: ["anthropic"]` 為 `agents: []`。修正 `mark_sync_meta_dirty` 無 sidecar 時寫 `dirty: false`（無 target 不標 dirty）;v2 sidecar 時依 pushable target 存在性決定 dirty。修正 `skill_targets_set` dirty 從無條件 true 改為 `targets.iter().any(|t| t.enabled && mode == tracked)`。修正 `skill_sync_one` 空 results 也清 dirty。Verify: cargo test 61 passed。
- [x] 7.2 `PendingPushBar` dirtyCount 過濾 `targets.length > 0`;`SkillList` 移除 Re-push 按鈕（dirty=false 時不顯示 push 控制項）。Verify: `npm run check` 0 error。
- [x] 7.3 TargetEditor 移至 SkillEditor 上方（建立與編輯模式皆同）。建立 skill 時 TargetEditor 以 buffered 模式（`onTargetsChange` prop）顯示,Save 後先建 skill 再呼叫 `skill_targets_set` 寫入 pending targets。`SkillEditor.onSaved` 改為支援 async。Verify: `npm run check` 0 error。
- [x] 7.4 `syncOne` push 後改呼叫 `loadEntries()` 取代 partial optimistic update,確保 `lastSync` per-target 記錄即時刷新。Verify: `npm run check` 0 error。
- [x] 7.5 `SkillList` agent chips 改從 `skill.targets` 取得去重 agent 列表。Verify: `npm run check` 0 error。
- [x] 7.6 `skill_prune_orphans_apply` 參數從 `paths: Vec<String>` 改為 `orphans: Vec<OrphanFile>`;刪除後清除 sync-meta 對應 `lastSync` 記錄。`OrphanFile` 加 `Deserialize`。前端 `api.skillPrune.apply` 對應更新。Verify: cargo test 61 passed;`npm run check` 0 error。
- [x] 7.7 TargetEditor segmented control 從 `Tracked / Detached / Disabled` 改為 `Tracked / Disabled` 兩態;Detached（tooltip「Phase 2: drift detection」）與 Forked（tooltip「Phase 2: overlay rendering」）均 disabled 佔位。Verify: `npm run check` 0 error。

## 8. 手動 smoke（使用者書面回報）

- [x] 8.1 [P] 手動 smoke:`npm run tauri dev` 啟動;(a) 點 New skill → TargetEditor 在上方顯示(buffered 模式)、Prune orphans 隱藏;(b) 建立中加 target → 列表即時更新;(c) 填 name/description → Save → skill + targets 一步建立、PendingPushBar 出現;(d) 不加 target 建 skill → PendingPushBar 不出現;(e) 有 pushable target → Push 按鈕藍色顯示、Push all bar 顯示;(f) Push 成功後 → 按鈕消失、bar 消失、Sync info 更新;(g) 全部 target 切 Disabled → Push 按鈕消失、bar 消失;(h) 無 target 的 skill → 列表無 Push 按鈕;(i) Add target anthropic global → Push 寫入 agent 目錄;(j) 切 Disabled → Push 跳過;(k) SkillEditor 無 agents 控制項、save 後 agents 欄位不變;(l) Prune orphans → ConfirmDialog 列路徑 → 確認後刪除、Sync info 更新;(m) Project scope → known-projects 下拉只有當前 project 可選。Verify: 使用者書面回報 (a)–(m) 行為符合預期。
