## 1. Baseline 與資料型別骨架

- [x] 1.1 建立健全度基線:跑 `npm run check` 與 `cd src-tauri && cargo build`,將 stdout / stderr 與 exit code 寫入 `openspec/changes/multi-agent-skills-foundation/baseline.txt`。Verify: 該檔存在;npm 段記錄當前 error 數作為 diff 基準、cargo 段 exit 0。
- [x] 1.2 在 `src-tauri/src/paths.rs` 新增 canonical 目錄解析 helper(對應決策 1:Canonical 儲存目錄前綴採 `.glyphic/skills/`):`glyphic_global_skills_dir()` 回傳 `~/.glyphic/skills/`、`glyphic_project_skills_dir(project)` 回傳 `<project>/.glyphic/skills/`。Verify: `cargo build` exit 0;新 helper 在 unit-style assert(或臨時 `cargo test`)下回傳預期路徑,Windows 與 Unix 分隔符皆正確。
- [x] 1.3 在 `src/lib/types/skills.ts` 定義 canonical 前端型別(對應決策 2:snake_case canonical schema):`CanonicalSkill`(name / description / agents / frontmatterExtras / body / dirty / lastSynced)、`SyncResult`、`ImportCandidate`、`ConflictInfo`、`ImportSelection`、`AgentPathsConfig`、`AgentPathPair`。Verify: `npm run check` 對該檔 0 error。

## 2. 後端 canonical CRUD（Requirement: Canonical Skill Storage）

- [x] 2.1 新增 `src-tauri/src/commands/canonical_skills.rs`,實作 `CanonicalSkill` Rust struct 與 frontmatter parse(滿足 Requirement: Canonical Skill Storage;對應決策 2:required 三欄位強型別 + `frontmatter_extras` 用 `serde_yaml::Value` 通透保存,body raw passthrough)。在 `src-tauri/Cargo.toml` 新增 `serde_yaml` 依賴。Verify: `cargo build` exit 0;餵一份含 snake_case frontmatter 的 SKILL.md 字串,parse 後 name / description / agents 正確、未知欄位保留在 extras。
- [x] 2.2 實作 `canonical_skills_list` / `canonical_skills_read` command(對應決策 1):依 scope 掃 `.glyphic/skills/`,目錄不存在回空 Vec,frontmatter parse 失敗的 skill 標為 broken 而非整批失敗。Verify: 手動建兩個 skill 目錄 + 一個壞 frontmatter,list 回三筆且壞的那筆有 broken 標記;不存在的 scope 回空 Vec 無 error。
- [x] 2.3 實作 `canonical_skills_write` / `canonical_skills_delete` command(對應決策 1):write 時 `.glyphic/skills/` 不存在自動 `create_dir_all`;skill name 含 path 段(例如上層目錄 traversal)時拒絕寫入。Verify: 對全新 `~/.glyphic/skills/` 寫第一個 skill 成功建目錄;傳入惡意 name 被拒。

## 3. 後端 fan-out renderer（Requirement: Fan-Out to Agent Targets）

- [x] 3.1 新增 `src-tauri/src/commands/fan_out/mod.rs`,定義共用 `FanOutRenderer` trait(滿足 Requirement: Fan-Out to Agent Targets;輸入 `CanonicalSkill` + target dir,輸出寫檔結果)與三家 dispatch(對應決策 3:Module 結構 canonical_skills.rs + fan_out 子模組)。Verify: `cargo build` exit 0;trait 有三個具名 impl 註冊點。
- [x] 3.2 [P] 實作 `src-tauri/src/commands/fan_out/anthropic.rs`(對應 agent-skills-schema 的 Anthropic mapping):snake_case → kebab-case rename,輸出單一 `.claude/skills/<name>/SKILL.md`。Verify: 餵三家全選的 canonical,Anthropic 輸出檔 frontmatter 為 kebab-case(如 `allowed-tools`)、body 一致。
- [x] 3.3 [P] 實作 `src-tauri/src/commands/fan_out/codex.rs`(對應 Codex split-file mapping):`SKILL.md` 僅含 name + description,UI metadata 拆到 sibling `agents/openai.yaml`(interface.display_name / short_description / default_prompt)。Verify: 輸出兩個檔案,SKILL.md frontmatter 只有 name + description,openai.yaml 含 interface 區塊。
- [x] 3.4 [P] 實作 `src-tauri/src/commands/fan_out/gemini.rs`(對應 Gemini minimal mapping):`SKILL.md` 只複製 name + description,其餘欄位 ignore;目標路徑用使用者設定的 `.gemini/skills/` 或 alias。Verify: 輸出檔只有 name + description,Anthropic-only 欄位(如 effort)不出現。
- [x] 3.5 實作 `skill_sync_one` / `skill_sync_all` command(對應決策 5 的 push 動作層 + 決策 3 dispatch):依 skill 的 `agents` 欄位 dispatch 到對應 renderer,target 不存在自動建立,單一 target 失敗回 `SyncResult.success=false` 但其他 target 繼續。Verify: agents=[anthropic] 只寫 Anthropic 目錄;模擬一個 target 不可寫,回傳的 Vec 該筆 success=false、其餘 true。

## 4. 後端 import 與 agent paths（Requirement: Initial Skill Import + Settings Page Agent Paths Section）

- [x] 4.1 新增 `src-tauri/src/commands/skill_import.rs`,實作 `skill_import_scan_quick`(滿足 Requirement: Initial Skill Import;對應決策 6:被動偵測):數三家 agent 目錄子目錄數,不讀內容。Verify: 預放 N 個 agent skill 目錄,scan_quick 回正確計數且不含 body。
- [x] 4.2 實作 `skill_import_scan` / `skill_import_apply` command(對應決策 6:手動精靈 + 衝突解決):scan 細掃回 `ImportCandidate`(含同名衝突的 diff summary),apply 依使用者 `ImportSelection` 寫 canonical、原 agent 檔不刪。讀檔前對 source path normalize 防 traversal。Verify: 預放含同名衝突的來源,scan 回 conflict 資訊;apply 後 canonical 出現、原檔仍在。
- [x] 4.3 實作 `agent_paths_get` / `agent_paths_set` command(滿足 Requirement: Settings Page Agent Paths Section 的後端;對應決策 8:Settings agent paths 可覆寫 + 路徑驗證):預設值來自 schema-reference,持久化到 `~/.claude/settings.json` 自訂 key;set 時 reject 含上層目錄 traversal / 絕對路徑越界,失敗回 Err。Verify: get 無設定時回 schema 預設;set 一個合法路徑後 get 回新值;set 惡意路徑回 Err 且不寫入。

## 5. 命令註冊與舊命令移除

- [x] 5.1 在 `src-tauri/src/commands/mod.rs` 註冊 `canonical_skills` / `fan_out` / `skill_import` 模組,在 `src-tauri/src/lib.rs` 的 `invoke_handler!` 註冊全部新 command。Verify: `cargo build` exit 0;新 command 可被 invoke(dev 環境呼叫不回 "command not found")。
- [x] 5.2 移除舊 skill command(對應 What Changes:廢除 list_skills / write_skill / delete_skill):從 `src-tauri/src/commands/skills.rs` 移除三條 skill command 與其專屬 helper,**保留** list_agents / write_agent / delete_agent(對應決策 7:AGENT.md 子系統不動);從 `invoke_handler!` 移除已刪 command。Verify: `cargo build` exit 0、無 dead-code warning;`list_agents` 等三條仍註冊可用。
- [x] 5.3 同步前端 wrapper:`src/lib/tauri/commands.ts` 移除 listSkills / writeSkill / deleteSkill,新增 canonical CRUD + sync + import + agentPaths wrapper。Verify: `npm run check` 對該檔 0 error;移除的 wrapper 無殘留引用。

## 6. 前端 store 與 Skills 頁重寫（Requirement: Pending-Push Sync State + Visual Frontmatter Editor）

- [x] 6.1 新增 `src/lib/stores/skills-store.ts`(滿足 Requirement: Pending-Push Sync State;對應決策 5:dirty bit 狀態管理):維護 canonical 清單、per-skill dirty Map、lastSynced Map、import banner dismissed 旗標;dirty 狀態持久化到各 skill 目錄的 sync-meta JSON。Verify: `npm run check` 0 error;store action 在元件外可被呼叫設定 / 清除 dirty。
- [x] 6.2 [P] 新增 `src/lib/components/skills/SkillList.tsx`(對應決策 5:per-skill dirty dot + push):列表顯示 name / description / agents 標籤 / dirty 紅點 / per-skill Push 按鈕,scope filter 切 global / project。Verify: `npm run tauri dev` 手動:列表正確渲染、dirty 紅點隨 store 變動、單列 Push 觸發 sync。
- [x] 6.3 [P] 新增 `src/lib/components/skills/PendingPushBar.tsx`(對應決策 5:sticky pending-push bar):頂端固定列顯示 dirty 數與 Push all,dirty 為 0 時不顯示。Verify: 手動:兩個 skill 改過時 bar 顯示 "2 skills changed"、Push all 後消失。
- [x] 6.4 [P] 新增 `src/lib/components/skills/SkillEditor.tsx`(滿足 Requirement: Visual Frontmatter Editor;對應決策 4:UI 完全屏蔽 raw YAML):Properties 視覺化表單(每欄位一控制項)+ Advanced collapsible(低頻欄位,預設折疊)+ Markdown body textarea;無 raw YAML tab。表單 serialize / deserialize canonical snake_case。Verify: 手動:開啟 skill 看到表單非 YAML、Advanced 預設折疊、存檔後 `.glyphic/skills/<name>/SKILL.md` 為 snake_case。
- [x] 6.5 [P] 新增 `src/lib/components/skills/SkillImportBanner.tsx` 與 `SkillImportWizard.tsx`(滿足 Requirement: Initial Skill Import 的前端;對應決策 6:banner + 精靈):banner 在 canonical 空且偵測到既有 skill 時顯示、可 dismiss(持久化);wizard 呈現候選 + 同名衝突 diff + 四種 resolution。Verify: 手動:預放 fake-skill 到 `~/.claude/skills/`,啟動見 banner、import 後 canonical 出現 agents=[anthropic] 的 fake-skill、原檔仍在、dismiss 後不再顯示。
- [x] 6.6 重寫 `src/lib/components/skills/SkillsPage.tsx`(整合 6.2–6.5):組合 ImportBanner + PendingPushBar + SkillList + SkillEditor,取代原 215 行 CRUD。Verify: `npm run check` 0 error;`npm run tauri dev` Skills 頁完整動線(list → edit → save → dirty → push)可走通。

## 7. Settings 頁 Agent Paths（Requirement: Settings Page Agent Paths Section）

- [x] 7.1 新增 `src/lib/components/settings/AgentPathsSection.tsx` 並掛進 `src/lib/components/settings/SettingsPage.tsx`(滿足 Requirement: Settings Page Agent Paths Section 的前端;對應決策 8):collapsible 區,三家 × global / project 共 6 欄位,預設帶 schema 值,每欄顯示偵測到的 skill 數,只暴露三家。Verify: `npm run check` 0 error;`npm run tauri dev`:6 欄位顯示預設、改 Gemini path 為 alias 後 push 落在新路徑、輸入 traversal 路徑被拒並 fall back。

## 8. 安全審查與最終驗證

- [x] 8.1 跑 `/spectra-audit`(對應 design 安全敏感性評估:讀寫使用者檔案系統 + 處理使用者輸入路徑):重點審 `agent_paths_set` 路徑驗證、`canonical_skills_write` 的 skill name 注入、fan-out renderer 寫檔前 sandbox check、import source path normalize。Verify: audit 報告無 Critical;發現的 sharp edge 已在對應 command 修正並重審通過。
- [x] 8.2 最終驗證:對照 `baseline.txt` 跑 `npm run check`(不得新增 baseline 外的 error)與 `cd src-tauri && cargo build`(exit 0、無新 warning);跑 `spectra validate multi-agent-skills-foundation` 與 `spectra analyze` 確認 Critical / Warning = 0;`npm run tauri dev` 手動走 design Acceptance criteria 的五個 smoke 步驟。Verify: 三項自動檢查通過 + 五個 smoke 步驟人工確認 OK。
- [x] 8.3 Smoke 驗收完成(對應 Addendum E 的 smoke verification 重構):新增 2 個 cargo unit test 在 `src-tauri/src/commands/fan_out/mod.rs::tests` 完整覆蓋 smoke #2(三家全推 + bundled siblings 鏡射 + sync-meta 不洩漏到 target)與 #3(dirty=true sidecar 在 push 成功後翻為 dirty=false 加 last_synced 落值,canonical_skills_list 同步反映);smoke #1 與 #5 後端契約已分別由 `canonical_skills::tests::write_creates_dir_and_round_trips_through_read` 與 `agent_paths::tests::validate_rejects_traversal` 覆蓋,本 task 不重複建立。同時由使用者跑一輪 `npm run tauri dev` UI spot-check 確認 Addendum B / C 列舉的 5 個 UI 行為(SkillEditor Save 位置、bundled files 可見、Per-skill Push 按鈕 label flip、Reload spinner、Settings → Agent Paths Gemini path 編輯與 reject 回饋)無視覺或互動退化。Verify: `cd src-tauri && cargo test --lib` 從 29 passing → 31 passing(新 2 test 通過、原 29 test 未退化);使用者書面回報 UI spot-check 各項無 regression。
- [x] 8.6 Import 正確性修補(對應 Addendum H):修兩個 import 語意 bug。(a) bug 1 偵測計數:`src-tauri/src/commands/skill_import.rs::skill_import_scan_quick` 的 `total` 由「逐資料夾相加」改為「跨 agent unique skill name 數」(同名 skill 在 .claude + .agents 算一個),per-agent 計數維持各 agent location 內 distinct names;新 helper `skill_names_at` / `distinct_count`。(b) bug 2 多來源延後:`skill_import_scan` 以 `group_by_name` 依 skill name 分組,單來源 skill 維持可 import(`deferred=None`),同名 skill 出現在 2+ agent 資料夾則收斂成一筆 `deferred=Some({agents,reason})`;`ImportCandidate` 與 `types/skills.ts` 加 `deferred` 欄位;`SkillImportWizard.tsx` 把 deferred row grey 化(opacity-60)+ 顯示 "found in <agents>" 與 reason、不渲染 resolution 控制、apply selection 排除 deferred;`skill_import_apply` 防禦性跳過 deferred candidate。多來源的正確解析(選來源/union/per-agent 客製)刻意延到 target-control change。Verify: `cargo test --lib` 從 32 → 35 passing(新增 `group_by_name_defers_multi_source_keeps_single_source` / `distinct_count_dedupes_shared_names` / `apply_skips_deferred_candidate`);`npm run check` 0 error;使用者 spot-check 確認偵測數顯示 unique 數、多來源 skill grey + 註記、單來源 skill 正常 import 且 tag 正確。
- [x] 8.5 Skills 頁 layout + page-scaffold 高度契約修補(對應 Addendum G):落實 4 個 fix。(a) `src/lib/components/skills/SkillsPage.tsx` body 包成 `h-full flex flex-col min-h-0`、list/editor grid 改 `flex-1 min-h-0`、兩欄各自 `overflow-y-auto`,達成兩欄獨立內捲。(b) `src/lib/components/skills/PendingPushBar.tsx` 去除 `sticky top-0 z-10 -mx-6`,改正常流 rounded banner,滾動時不再字體重疊。(c) page-scaffold 高度契約:`src/router.tsx` 的 Outlet wrapper 由 `flex-1 overflow-hidden` 改 `flex-1 overflow-hidden flex flex-col min-h-0`,`src/lib/components/shared/PageScaffold.tsx` 的 `PageBody` 由 `h-full` 改 `flex-1 min-h-0`,修正 PageHeader + PageBody sibling 高度溢出導致底緣被 overflow-hidden 裁切;此為 shared scaffold 變動,須確認另外 4 個 routed 頁(settings / tokens / templates / memory)在 flex-col 容器下無破版。(d) `src/lib/components/skills/SkillList.tsx` 排序改 dirty(含 broken)置頂、同組內字母序。Verify: `npm run check` 0 error;使用者 spot-check 確認 Skills 兩欄底緣 border 可見、各自內捲、pending bar 無重疊、dirty 置頂排序生效,且 settings / tokens / templates / memory 四頁無 layout regression。
- [x] 8.4 UI spot-check regression 修補(對應 Addendum F):落實 Addendum F 列舉的 4 個 fix。(a) `src/lib/components/skills/SkillEditor.tsx` 的 Name input / Description textarea / Body textarea 加 `w-full`,Body 同時加 `resize-y`,讓表單控制項撐滿 editor pane 不再被 `overflow-hidden` 裁切。(b) `src/lib/components/settings/AgentPathsSection.tsx` 把 inline error tile 改為 centered alert modal(role=`alertdialog`, aria-modal),完整錯誤訊息以 mono font 顯示,backdrop 不可點關閉、需按 OK 才 dismiss;info 成功訊息仍 inline 不改。(c) `src-tauri/src/commands/canonical_skills.rs::canonical_skills_write` 寫完 SKILL.md 後呼叫新 helper `mark_sync_meta_dirty` 把 sync-meta sidecar `dirty` 設為 true 同時保留既有 `last_synced`,讓新建 / 編輯過的 canonical 在 list 顯示 dirty=true、Push 按鈕 label = `Push`。(d) `src/lib/components/skills/SkillsPage.tsx::handleDelete` 改用既有 `shared/ConfirmDialog` 元件取代 `window.confirm()`,confirm 訊息明示「agent 端複本不會被刪除」,維持頁面 modal 風格一致。Verify: `npm run check` 0 error;`cargo test --lib` 從 31 passing → 32 passing(新增 `canonical_skills::tests::write_marks_canonical_dirty_in_sync_meta` 涵蓋首寫 dirty=true + 第二次寫保留 last_synced 的 contract);使用者 spot-check 確認 (a)~(d) 四項顯著修復、無新退化。
